package com.example.demo_fit

import android.content.Intent

interface FragmentAux {
    fun refresh()
    abstract fun Intent(profileFragment: ProfileFragment, java: Class<UpdatePassword>): Intent?
}